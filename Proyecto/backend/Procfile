# backend/Procfile
web: bash -lc "python manage.py migrate --noinput && python manage.py bootstrap_admin || true && python manage.py collectstatic --noinput && gunicorn claro_project.wsgi:application --workers 2 --timeout 120 --bind 0.0.0.0:$PORT"
